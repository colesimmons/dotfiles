# Select command-line tools deliberately. Comment-only files install nothing.
# Syntax example (inactive): brew "ripgrep"

# TODO: GitHub authentication tools and everyday command-line utilities.
# Fish and Starship are selected in Brewfile.shell.
# TODO: Choose runtime ownership (mise, uv, or alternatives) before adding tools.
# TODO: Add container, database, and deployment tools only for active workflows.
# Keep project runtime versions and project dependencies in their project repos.
